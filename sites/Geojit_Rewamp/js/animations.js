/*   
 * Template Name: Responsive  Golf Club Template
 * Version: 1
 * Author: Binoy VM
 * Website: http://www.neointeraction.com/
*/
/* ======= Base ======= */

/* ======= Animations ======= */
$(document).ready(function() {
     if (isMobile.any === false) { 
   
        /* Animate elements in #bg-slider */
        $('.banner .banner-text h1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('animated fadeInDown delayp1');}
        });
        $('.banner .banner-text h3').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('animated fadeInDown delayp5');}
        });
        $('.banner .banner-text .btn-get-started').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('animated fadeInLeft delayp5');}
        });
        $('.live-comparison .gain-stocks  ').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('animated fadeInDown delayp5');}
        });
        $('.live-comparison .details-number  ').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('animated fadeInDown delayp5');}
        });
        $('.live-comparison .details-number  ').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('animated fadeInDown delayp5');}
        });
        $('.live-comparison .details-number  ').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('animated fadeInDown delayp5');}
        });
        }


          $('.news .item .thumb1 .thumb-image').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
          $('.news .item .thumb2 .thumb-image').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
          $('.news .item .thumb3 .thumb-image').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
        $('.callback-request .container').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
        $('.ad_block .ad-img-1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
        $('.ad_block .ad-img-2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
        $('.ad_block .ad-img-3').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
         $('.boxed-nav ul').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('animated fadeIn delayp5');}
        });
          $('.bod1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
        $('.bod2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
         $('.bod3').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
          $('.bod4').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
           $('.bod5').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.bod6').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
         $('.bod7').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
          $('.mgm1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
           $('.mgm2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.mgm3').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
             $('.mgm4').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
        $('.mgm5').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
         $('.wt1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
          $('.wt2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
          $('.gl1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
          $('.gl2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
           $('.fp1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.fp2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.rf1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.rf2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.rf3').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.rf4').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
             $('.gf1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.gf2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.gf3').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.gf4').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });

              $('.mfw1').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.mfw2').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.mfw3').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });
            $('.mfw4').one('inview', function(isInView) {
            if (isInView) {$(this).addClass('fade-in-lazy');}
        });








         
  
        
});

var dropdownSelectors = $('.dropdown, .dropup');

// Custom function to read dropdown data
// =========================
function dropdownEffectData(target) {
  // @todo - page level global?
  var effectInDefault = null,
      effectOutDefault = null;
  var dropdown = $(target),
      dropdownMenu = $('.dropdown-menu', target);
  var parentUl = dropdown.closest('ul.nav'); 

  // If parent is ul.nav allow global effect settings
  if (parentUl.length > 0) {
    effectInDefault = parentUl.data('dropdown-in') || null;
    effectOutDefault = parentUl.data('dropdown-out') || null;
  }
  
  return {
    target:       target,
    dropdown:     dropdown,
    dropdownMenu: dropdownMenu,
    effectIn:     dropdownMenu.data('dropdown-in') || effectInDefault,
    effectOut:    dropdownMenu.data('dropdown-out') || effectOutDefault,  
  };
}

// Custom function to start effect (in or out)
// =========================
function dropdownEffectStart(data, effectToStart) {
  if (effectToStart) {
    data.dropdown.addClass('dropdown-animating');
    data.dropdownMenu.addClass('animated');
    data.dropdownMenu.addClass(effectToStart);    
  }
}

// Custom function to read when animation is over
// =========================
function dropdownEffectEnd(data, callbackFunc) {
  var animationEnd = 'webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend';
  data.dropdown.one(animationEnd, function() {
    data.dropdown.removeClass('dropdown-animating');
    data.dropdownMenu.removeClass('animated');
    data.dropdownMenu.removeClass(data.effectIn);
    data.dropdownMenu.removeClass(data.effectOut);
    
    // Custom callback option, used to remove open class in out effect
    if(typeof callbackFunc == 'function'){
      callbackFunc();
    }
  });
}

// Bootstrap API hooks
// =========================
dropdownSelectors.on({
  "show.bs.dropdown": function () {
    // On show, start in effect
    var dropdown = dropdownEffectData(this);
    dropdownEffectStart(dropdown, dropdown.effectIn);
  },
  "shown.bs.dropdown": function () {
    // On shown, remove in effect once complete
    var dropdown = dropdownEffectData(this);
    if (dropdown.effectIn && dropdown.effectOut) {
      dropdownEffectEnd(dropdown, function() {}); 
    }
  },  
  "hide.bs.dropdown":  function(e) {
    // On hide, start out effect
    var dropdown = dropdownEffectData(this);
    if (dropdown.effectOut) {
      e.preventDefault();   
      dropdownEffectStart(dropdown, dropdown.effectOut);   
      dropdownEffectEnd(dropdown, function() {
        dropdown.dropdown.removeClass('open');
      }); 
    }    
  }, 
});


